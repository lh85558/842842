
# 故障排除指南

## 常见问题快速解决

### 🔧 基础检查清单
- [ ] 路由器电源正常
- [ ] 网线连接正确
- [ ] IP地址设置正确（192.168.10.1）
- [ ] 固件文件完整
- [ ] 浏览器缓存已清理

### 🚨 紧急情况处理

#### 路由器无法启动
1. **断电重启**
   ```bash
   # 拔掉电源，等待30秒
   # 重新插上电源
   # 观察指示灯状态
   ```

2. **恢复出厂设置**
   ```bash
   # 按住 Reset 按钮 10 秒
   # 等待路由器重启
   # 重新配置
   ```

3. **TFTP 救砖**
   - 参考 [FLASH_GUIDE.md](FLASH_GUIDE.md) 中的 TFTP 方法

## 刷写问题

### 刷写失败

#### 症状：Web界面刷写卡住
**解决方案：**
```bash
# 1. 检查固件文件
md5sum factory.bin
# 对比 MD5 值是否匹配

# 2. 清理浏览器缓存
# 3. 使用不同浏览器
# 4. 尝试 TFTP 方法
```

#### 症状：刷写后无法启动
**解决方案：**
```bash
# 1. 检查指示灯
# 电源灯：常亮
# 系统灯：闪烁后常亮
# 网络灯：连接后亮

# 2. 尝试串口救砖
# 3. 联系技术支持
```

#### 症状：变砖（无任何反应）
**解决方案：**
```bash
# 1. 准备串口工具
# 2. 连接串口线
# 3. 查看启动信息
# 4. 使用 TFTP 重新刷写
```

### 固件验证

#### 检查固件完整性
```bash
# 计算 MD5
md5sum output/*factory.bin

# 检查文件大小
ls -lh output/*factory.bin

# 验证固件信息
file output/*factory.bin
```

#### 验证刷写结果
```bash
# SSH 登录
ssh root@192.168.10.1

# 检查系统信息
cat /etc/openwrt_release

# 检查分区
df -h

# 检查内存
free -m
```

## 网络问题

### 无法访问管理界面

#### 症状：页面无法打开
**排查步骤：**
```bash
# 1. 检查 IP 配置
ip addr show

# 2. 测试连通性
ping 192.168.10.1

# 3. 检查端口
telnet 192.168.10.1 80
telnet 192.168.10.1 443

# 4. 检查防火墙
iptables -L -n
```

#### 症状：登录失败
**解决方案：**
```bash
# 1. 确认用户名密码
# 默认：admin / thdn12345678

# 2. 重置密码
# 通过 SSH 登录
passwd admin

# 3. 清除浏览器数据
# 4. 尝试无痕模式
```

### WiFi 问题

#### 症状：WiFi 无法连接
**解决方案：**
```bash
# 1. 检查 WiFi 状态
iwconfig

# 2. 重启 WiFi
wifi down
wifi up

# 3. 检查配置
uci show wireless

# 4. 重新配置
rm -f /etc/config/wireless
wifi config
```

#### 症状：WiFi 信号弱
**解决方案：**
```bash
# 1. 调整信道
uci set wireless.radio0.channel=6
uci commit wireless
wifi

# 2. 调整功率
uci set wireless.radio0.txpower=20
uci commit wireless
wifi

# 3. 检查天线连接
```

### 网络连接不稳定

#### 症状：频繁断网
**解决方案：**
```bash
# 1. 检查网络配置
uci show network

# 2. 重启网络服务
/etc/init.d/network restart

# 3. 检查 DHCP
/etc/init.d/dnsmasq restart

# 4. 查看系统日志
logread | grep network
```

## 打印问题

### 打印机识别问题

#### 症状：USB 打印机无法识别
**排查步骤：**
```bash
# 1. 检查 USB 连接
lsusb

# 2. 检查 USB 模块
lsmod | grep usb

# 3. 查看系统日志
dmesg | tail -50

# 4. 重启 CUPS
/etc/init.d/cups-setup restart
```

#### 症状：打印机离线
**解决方案：**
```bash
# 1. 检查打印机状态
lpstat -p

# 2. 重新启用打印机
cupsenable 打印机名

# 3. 接受打印任务
cupsaccept 打印机名

# 4. 重启打印服务
/etc/init.d/cups-setup restart
```

### 打印质量问题

#### 症状：打印模糊/不清晰
**解决方案：**
```bash
# 1. 清洁打印头
# 通过打印机维护工具

# 2. 调整打印质量
# CUPS 管理界面 → 打印机 → 设置

# 3. 检查墨盒/硒鼓
# 更换耗材

# 4. 校准打印机
# 使用打印机自带校准功能
```

#### 症状：打印速度慢
**优化方法：**
```bash
# 1. 使用 RAW 协议
# 网络打印设置 → 协议：RAW

# 2. 降低打印质量
# 设置为草稿模式

# 3. 检查网络速度
ping 192.168.10.1

# 4. 优化 CUPS 设置
vi /etc/cups/cupsd.conf
# 添加：MaxJobs 50
```

### 打印服务崩溃

#### 症状：CUPS 服务停止
**解决方案：**
```bash
# 1. 检查服务状态
/etc/init.d/cups-setup status

# 2. 查看错误日志
tail -f /var/log/cups/error_log

# 3. 重启服务
/etc/init.d/cups-setup restart

# 4. 检查内存
free -m
```

#### 症状：打印任务卡住
**解决方案：**
```bash
# 1. 查看打印队列
lpq

# 2. 取消所有任务
cancel -a

# 3. 重启打印机
# 断电重启

# 4. 清除缓存
rm -rf /var/spool/cups/*
```

## 系统问题

### 性能问题

#### 症状：系统运行缓慢
**诊断方法：**
```bash
# 1. 检查 CPU 使用率
top

# 2. 检查内存使用
free -m

# 3. 检查磁盘空间
df -h

# 4. 查看进程
ps aux | head -20
```

#### 症状：内存不足
**解决方案：**
```bash
# 1. 停止不必要的服务
/etc/init.d/dnsmasq stop
/etc/init.d/firewall stop

# 2. 清理缓存
echo 1 > /proc/sys/vm/drop_caches

# 3. 删除日志
rm -f /var/log/*.log

# 4. 重启系统
reboot
```

#### 症状：存储空间不足
**清理方法：**
```bash
# 1. 清理软件包缓存
opkg clean

# 2. 删除临时文件
rm -rf /tmp/*

# 3. 查找大文件
du -sh /* | sort -hr | head -10

# 4. 卸载不需要的软件
opkg list-installed | grep -v essential
```

### 服务问题

#### 症状：服务无法启动
**排查步骤：**
```bash
# 1. 检查服务状态
/etc/init.d/servicename status

# 2. 查看错误信息
/etc/init.d/servicename start
echo $?

# 3. 检查配置文件
uci show servicename

# 4. 查看系统日志
logread | grep servicename
```

#### 症状：定时任务不执行
**解决方案：**
```bash
# 1. 检查 cron 服务
/etc/init.d/cron status

# 2. 查看定时任务
crontab -l

# 3. 检查任务格式
# 确保格式正确：分 时 日 月 周 命令

# 4. 重启 cron 服务
/etc/init.d/cron restart
```

### 配置问题

#### 症状：配置丢失
**恢复方法：**
```bash
# 1. 检查配置文件
ls -la /etc/config/

# 2. 恢复默认配置
cp /rom/etc/config/network /etc/config/
cp /rom/etc/config/wireless /etc/config/

# 3. 重新配置
uci set network.lan.ipaddr=192.168.10.1
uci commit network
/etc/init.d/network restart
```

#### 症状：无法保存配置
**解决方案：**
```bash
# 1. 检查磁盘空间
df -h

# 2. 检查文件权限
ls -la /etc/config/

# 3. 重新挂载文件系统
mount -o remount,rw /

# 4. 手动保存配置
uci commit
```

## 高级故障排除

### 内核崩溃

#### 症状：系统不断重启
**解决方案：**
```bash
# 1. 进入安全模式
# 按住 reset 按钮启动

# 2. 检查内核日志
dmesg | grep -i error

# 3. 检查模块冲突
lsmod | grep -v usb

# 4. 重新刷写固件
# 使用 TFTP 方法
```

### 网络栈问题

#### 症状：网络完全失效
**解决方案：**
```bash
# 1. 重置网络配置
rm -f /etc/config/network
rm -f /etc/config/wireless
wifi config

# 2. 重新配置网络
uci set network.lan.proto=static
uci set network.lan.ipaddr=192.168.10.1
uci commit network

# 3. 重启网络服务
/etc/init.d/network restart
```

### USB 子系统问题

#### 症状：USB 完全失效
**解决方案：**
```bash
# 1. 检查 USB 模块
lsmod | grep usb

# 2. 重新加载模块
rmmod usblp
rmmod usbcore
modprobe usbcore
modprobe usblp

# 3. 检查硬件
lsusb -v

# 4. 重启 USB 服务
/etc/init.d/usb restart
```

## 日志分析

### 系统日志
```bash
# 查看系统日志
logread

# 查看内核日志
dmesg

# 查看启动日志
dmesg | grep -i error

# 查看服务日志
logread | grep servicename
```

### CUPS 日志
```bash
# 查看 CUPS 错误日志
tail -f /var/log/cups/error_log

# 查看 CUPS 访问日志
tail -f /var/log/cups/access_log

# 查看 CUPS 页面日志
tail -f /var/log/cups/page_log
```

### 网络日志
```bash
# 查看网络日志
logread | grep network

# 查看 DHCP 日志
logread | grep dhcp

# 查看 WiFi 日志
logread | grep wifi
```

## 恢复出厂设置

### 软件恢复
```bash
# 1. 进入管理界面
# 系统 → 备份/升级 → 恢复出厂设置

# 2. 或者命令行
firstboot
reboot
```

### 硬件恢复
```bash
# 1. 断电
# 2. 按住 reset 按钮
# 3. 通电，保持 10 秒
# 4. 等待重启
```

## 获取帮助

### 自助资源
1. **查看日志** - 系统会记录大部分错误
2. **搜索文档** - 参考用户手册和构建指南
3. **检查配置** - 确认所有设置正确
4. **重启服务** - 很多问题是临时性的

### 社区支持
- OpenWrt 官方论坛
- 恩山无线论坛
- GitHub Issues
- QQ 群/微信群

### 技术支持
如无法解决问题，请提供以下信息：
1. 路由器型号和硬件版本
2. 固件版本和构建日期
3. 详细的错误描述
4. 相关日志文件
5. 已尝试的解决方案

---

**记住：大多数问题都有解决方案，保持耐心！** 🔧
